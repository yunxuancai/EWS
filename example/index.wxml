<view class="container" bindtouchstart="touchStart" bindtouchmove="touchMove" bindtouchend="touchEnd">

<view class="page">
    <view class="page__hd">
        <view class="page__title">上传一幅图片以生成结果</view>
        <view class="page__desc">向左滑动可以切换模式</view>

                <view class="choose-upload" bindtap="chooseImage">
                    <image src="{{fileSrc}}" class="upload-img" wx:if="{{fileSrc}}" mode = 'widthFix'></image>
                </view>
    </view>
    <button bindtap="query" type="primary">确认</button>
    <view class="mid-text">
          
    </view>
    <view class="container" bindtouchstart="touchStart" bindtouchmove="touchMove" bindtouchend="touchEnd">
    <view class="table"><view class="tr">

    <view class="th"></view>
    </view></view>
    
</view>

</view>

</view>